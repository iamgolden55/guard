FROM node:20-slim

WORKDIR /app

COPY frontend/package.json frontend/package-lock.json* ./
RUN npm ci

# Source files (src/, public/, configs, .env) are bind-mounted from
# docker-compose.yml so changes hot-reload without rebuilding the image.

EXPOSE 3000
CMD ["npm", "run", "dev"]
