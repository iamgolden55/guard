/**
 * Theme Hook
 * Re-export useTheme from ThemeContext for convenience
 */

export { useTheme, getThemeColors, lightColors, darkColors } from '../contexts/ThemeContext';
export type { ThemeColors } from '../contexts/ThemeContext';
